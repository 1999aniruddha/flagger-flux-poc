apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: podinfo
  namespace: default
spec:
  hosts:
  - "*"
  gateways:
  - podinfo-gateway
  http:
  - route:
    - destination:
        host: podinfo
        port:
          number: 80